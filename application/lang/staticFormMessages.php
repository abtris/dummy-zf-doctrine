<?php
/**
 *  @version ##VERSION##
 *  @package ##PACKAGE##
 *  @filesource
 */
/**
 * static messages, in most cases, these are in the framework source code as
 * messages ids, or as form labels. It causes that these cannot be automatically
 * scanned through xgettext, so we enumerate these by default and gettext will
 * work.
 */
_('isEmpty');
_('emailAddressInvalid');
/* Form_Hash */
_('notSame');
_('missingToken');